services:
  queue:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - queue-data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/healthz"]
      interval: 5s
      timeout: 2s
      retries: 5

volumes:
  queue-data:
